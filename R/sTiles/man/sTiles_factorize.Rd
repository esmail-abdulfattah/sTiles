% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sTiles.R
\name{sTiles_factorize}
\alias{sTiles_factorize}
\title{Numeric Cholesky factorization, reusing the preprocessing}
\usage{
sTiles_factorize(x, Q = NULL)
}
\arguments{
\item{x}{An "sTiles" object from [sTiles_analyze()] or [sTiles()].}

\item{Q}{Optional: a matrix with the SAME sparsity pattern, whose values to
factor. When omitted, the values captured at analyze time are used.}
}
\value{
The "sTiles" object, now factorized, invisibly.
}
\description{
Runs the numeric phase on an object that [sTiles_analyze()] has already
prepared, so the ordering and tile layout are not recomputed.
}
\examples{
if (sTiles_available()) {
Q <- Matrix::bandSparse(50, k = c(0, 1),
                        diagonals = list(rep(4, 50), rep(-1, 49)),
                        symmetric = TRUE)
  s <- sTiles_analyze(Q)
  sTiles_factorize(s)
  sTiles_logdet(s)
  sTiles_close(s)
}
}
\seealso{
[sTiles_analyze()], [sTiles_update()]
}
