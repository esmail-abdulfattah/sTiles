% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sTiles.R
\name{sTiles_selinv_elm}
\alias{sTiles_selinv_elm}
\title{One entry of the selected inverse}
\usage{
sTiles_selinv_elm(x, i, j)
}
\arguments{
\item{x}{A factorized "sTiles" object built with `inverse = TRUE`.}

\item{i, j}{Row and column, 1-based, in the original ordering.}
}
\value{
The entry as a single number, 0 outside the factor pattern.
}
\description{
Returns the selected inverse at position `(i, j)` when that position lies in
the factor pattern, `pattern(L + L^T)`, and exactly 0 outside it. Both
triangles are accepted, since Z is symmetric. Triggers the selected-inverse
computation on first use.
}
\examples{
if (sTiles_available()) {
Q <- Matrix::bandSparse(50, k = c(0, 1),
                        diagonals = list(rep(4, 50), rep(-1, 49)),
                        symmetric = TRUE)
  s <- sTiles(Q, inverse = TRUE)
  sTiles_selinv_elm(s, 1, 2)
  sTiles_close(s)
}
}
\seealso{
[sTiles_selinv()], [sTiles_selinv_row()]
}
