% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sTiles.R
\name{sTiles_selinv_row}
\alias{sTiles_selinv_row}
\title{Several entries from one row of the selected inverse}
\usage{
sTiles_selinv_row(x, node, neighbors)
}
\arguments{
\item{x}{A factorized "sTiles" object built with `inverse = TRUE`.}

\item{node}{Row index, 1-based, in the original ordering.}

\item{neighbors}{Integer vector of column indices, 1-based.}
}
\value{
A numeric vector, one value per entry of `neighbors`.
}
\description{
Returns the selected inverse at `(node, k)` for each `k` in `neighbors`, the
access pattern a graph model wants. Entries outside the factor pattern come
back as 0.
}
\examples{
if (sTiles_available()) {
Q <- Matrix::bandSparse(50, k = c(0, 1),
                        diagonals = list(rep(4, 50), rep(-1, 49)),
                        symmetric = TRUE)
  s <- sTiles(Q, inverse = TRUE)
  sTiles_selinv_row(s, 5, c(4, 5, 6))
  sTiles_close(s)
}
}
\seealso{
[sTiles_selinv()], [sTiles_selinv_elm()]
}
