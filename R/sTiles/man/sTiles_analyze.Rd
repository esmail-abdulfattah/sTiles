% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sTiles.R
\name{sTiles_analyze}
\alias{sTiles_analyze}
\title{Preprocess a matrix: ordering bake-off and tile layout only}
\usage{
sTiles_analyze(
  Q,
  cores = 1L,
  mode = "auto",
  tile_size = 40L,
  inverse = FALSE,
  log_level = -1L
)
}
\arguments{
\item{Q}{A symmetric positive-definite matrix, either a
`Matrix::sparseMatrix` or a base matrix. Only the lower triangle is read.}

\item{cores}{Worker threads (default 1).}

\item{mode}{Tile regime: "auto" (default), "dense", "semisparse" or
"sparse".}

\item{tile_size}{Tile size, or -1 to let the solver choose (default 40).}

\item{inverse}{Reserve selected-inverse storage, required by
[sTiles_selinv()] and the `sTiles_selinv_*()` queries (default `FALSE`).}

\item{log_level}{Solver verbosity: -1 silent (default), 0 timing, 1 info,
2 debug, 3 trace.}
}
\value{
An object of class "sTiles", analyzed but not yet factorized.
}
\description{
The symbolic phase. It depends only on the sparsity pattern, not on the
numeric values, and performs no Cholesky. Follow it with [sTiles_factorize()]
for the numeric factorization. Timing the two apart separates the
preprocessing cost, paid once per pattern, from the numeric cost, paid once
per set of values.
}
\examples{
if (sTiles_available()) {
Q <- Matrix::bandSparse(50, k = c(0, 1),
                        diagonals = list(rep(4, 50), rep(-1, 49)),
                        symmetric = TRUE)
  s <- sTiles_analyze(Q)
  sTiles_factorize(s)
  sTiles_logdet(s)
  sTiles_close(s)
}
}
\seealso{
[sTiles_factorize()], [sTiles_update()], [sTiles()]
}
